# Test Models

Each subdirectory contains a 3D model used for testing SDL3D's loader
and renderer. Every model directory must include:

- The model file (`.glb` preferred, `.gltf` + assets also accepted)
- `LICENSE.md` with source URL, author, and license

## Models

| Directory | Description | License |
|-----------|-------------|---------|
| `simple_robot/` | Multi-mesh robot with node hierarchy | CC-BY 4.0 |
